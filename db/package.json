{
  "name": "finguru-backend",
  "version": "1.0.0",
  "description": "FinGuru financial management app - Node/Express + MongoDB backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "test": "jest --coverage",
    "test:unit": "jest --testPathPattern=unit",
    "test:database": "jest --testPathPattern=database",
    "test:functional": "jest --testPathPattern=functional",
    "test:integration": "jest --testPathPattern=integration"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.1.0"
  }
}
